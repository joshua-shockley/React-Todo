import React from 'react';

function TodoForm (props) { 
    
  return( 
      <div>
<input type="text" placeholder="...the todo"/>
<button>add the ToDo</button>
<button>clear the to ToDo list</button>  
</div>
); 
}

export default TodoForm;
